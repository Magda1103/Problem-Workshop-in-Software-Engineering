services:
  ai_engine:
    build: .
    container_name: pip_ai_system
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - ENV=development
    command: python src/data_pipline.py
    tty: true
    stdin_open: true