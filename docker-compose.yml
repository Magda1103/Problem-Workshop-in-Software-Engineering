services:
  api:
    build: .
    container_name: pip_api
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./models:/app/models
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"